health_checks:
  check_redis_connection:
      callable: rapidlib.health_check.tests.dummy_callables.verify_fileaccess
      parameters:
        host: localhost
        user: root
        passwd: PASSWORD
  check_mysql:
      callable: rapidlib.health_check.tests.dummy_callables.check_mysql

